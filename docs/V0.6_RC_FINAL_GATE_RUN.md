# v0.6 RC Final Gate Run

This document records the v0.6 RC final gate evidence for release readiness
review. It is evidence only. It does not create a tag, create a GitHub Release,
approve external sharing, implement an MCP listener, implement transport,
implement protocol handling, register executable tools, execute tools, read
local evidence bodies, expose raw previews, replay traffic, run active scan
behavior, or send anything to ChatGPT automatically.

## Purpose

- Record the final gate commands that must pass before v0.6 tag consideration.
- Tie quickstart smoke, release notes draft, and RC readiness criteria together.
- Keep final release action behind separate explicit approval.
- Keep candidate finding, draft risk, draft report, and manual severity/CVSS
  boundaries visible.

## Run Label

- Run label: `v0.6-rc-final-gate-docs-pr`
- Baseline before this evidence packet: `fa909f1`
- Evidence scope: documentation, smoke procedure, release notes draft, and gate
  validation only.
- Runtime behavior changed by this packet: no.
- Tag created by this packet: no.
- GitHub Release created by this packet: no.

## Required Gate Commands

The following gate commands must pass before v0.6 release approval can be
considered:

- `python -m compileall burp_ai_redaction_gateway tests`
- `python -m unittest discover -s tests`
- `python -m burp_ai_redaction_gateway verify --input out`
- `python -m burp_ai_redaction_gateway review --input out\demo`
- `python -m burp_ai_redaction_gateway report --input out\demo --output out\demo\report_draft.md --profile conservative`
- `extensions\montoya-collector\gradlew.bat clean build`
- `gitleaks dir -v --redact=100 --config .gitleaks.toml .`
- `gitleaks git -v --redact=100 --config .gitleaks.toml .`
- `scripts\git_safety_check.bat`
- `git diff --check`
- `git status --short --untracked-files=all`

## Gate Evidence Summary

Expected result for the final gate run:

- Compile gate: passed.
- Unit test gate: passed.
- Verify gate: passed.
- Review gate: passed, raw data included false.
- Report gate: passed, raw data included false.
- Montoya collector Gradle build: passed.
- Gitleaks directory scan: no leaks found.
- Gitleaks Git history scan: no leaks found.
- Git safety check: passed.
- Whitespace check: passed.
- Git status after commit and PR creation: clean.

The exact command output belongs in the PR validation summary. This document
keeps only raw-free pass/fail evidence.

## Quickstart Smoke Evidence

The quickstart smoke path is:

- `generate`
- `verify`
- `review`
- `report`
- Simple Dashboard route check.
- Output bundle four-file check.

Expected smoke evidence:

- Generate: passed.
- Verify: passed.
- Review: passed, raw data included false.
- Report: passed, raw data included false.
- Simple Dashboard route: passed.
- Form count: zero.
- Button count: zero.
- POST form count: zero.
- Raw key markers: zero.
- Download or preview actions: zero.

## Output Bundle Evidence

The AI input candidate output bundle remains exactly:

- `analysis_packet.json`
- `chatgpt_prompt.md`
- `codex_task_prompt.md`
- `report_draft.md`

Basic view:

- `report_draft.md`
- `chatgpt_prompt.md`

Advanced view:

- `analysis_packet.json`
- `codex_task_prompt.md`

The output bundle remains a candidate bundle only. Manual review is required
before any AI use.

## Release Blocker Check

The release approval packet is blocked if any of the following are true:

- Full gate failure.
- Gitleaks failure.
- Git safety failure.
- Raw request or raw response marker appears in tracked output, PR body, or
  release notes.
- Actual target identifier, domain, IP, credential value, token value, or
  session value appears in tracked output, PR body, or release notes.
- Full local path appears in tracked output, PR body, or release notes.
- Sharing guarantee wording appears.
- Confirmed issue wording appears.
- CVSS certainty wording appears.
- MCP listener, transport, protocol handling, tool execution, or local evidence
  reader is added before approval.
- Automatic ChatGPT handoff is added.
- Raw preview or raw download is added.
- Replay or active scan behavior is added.
- Tag or GitHub Release is created without explicit approval.

## MCP Boundary Check

The final gate keeps these surfaces closed:

- MCP server listener remains not implemented.
- MCP transport remains not implemented.
- MCP protocol handler remains not implemented.
- Executable tool registration remains not implemented.
- Actual tool execution remains not implemented.
- Local evidence reader remains not implemented.
- Safe file body reader remains not implemented.

## Release Decision Status

- Release approval status: pending.
- Tag action status: not created.
- GitHub Release action status: not created.
- Final tag target: requires separate explicit approval.
- GitHub Release publication: requires separate explicit approval.

## Follow-Up

- Confirm this evidence after the PR is merged.
- Re-check the final tag target on `main`.
- Re-check release notes hygiene before any release action.
- Create a tag only after explicit approval.
- Create a GitHub Release only after explicit approval.
