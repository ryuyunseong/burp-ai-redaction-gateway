# v0.6 Fast-Track Plan

This planning only document groups v0.6 work into reviewable slices. It now
tracks completed read-only UX slices as well as the next planning candidates.
It does not implement an MCP server, MCP transport, tool handler, local evidence
reader, dashboard POST action, upload or import action, collector forwarding
change, receiver ingest change, raw preview, replay, active scan, automatic
ChatGPT handoff, tag, or GitHub Release.

## Goals

- Keep the gateway local-first and raw-free at every user-facing boundary.
- Move quickly on read-only UX and documentation slices.
- Split security-sensitive runtime or state-changing work into separate PRs.
- Keep findings as candidates until manual review.
- Keep risk values as drafts until manual review.
- Keep severity and CVSS as manual decisions.
- Keep AI input limited to the four candidate safe files.

## Fast-Track Slices

These slices can move together when they stay read-only, metadata-only, and
covered by tests.

| Slice | Status | Purpose | Completion Criteria | Not In Scope |
| --- | --- | --- | --- | --- |
| Output alias selector follow-up UX | Completed in the read-only dashboard UX bundle. | Polish verified output navigation after the first selector PR lands. | Browser smoke confirms the selector is read-only and verified-output based. | POST actions, raw preview, file download, target details. |
| Troubleshooting panel read-only UX | Completed in the read-only dashboard UX bundle. | Show operator guidance for common verify, review, dashboard, and export friction. | The panel uses safe aliases, existing dashboard routes, and non-serving document filenames only. | Raw traffic display, local evidence intake, replay. |
| Release readiness status panel | Completed in the read-only dashboard UX bundle. | Summarize local readiness evidence for the current branch without changing state. | The panel shows raw-free metadata and does not create tags or releases. | Tag creation, GitHub Release creation, publish workflow. |
| MCP registry adapter design | Next candidate. | Define how read-only tools will resolve verified output aliases. | Contract text and fixtures describe deny-by-default behavior. | MCP server, transport, tool handler, local file reader. |
| MCP read-only prototype skeleton plan | Later candidate. | Prepare a small implementation plan before adding runtime transport. | The plan lists fixtures, allowed tool names, and blocked response fields. | Network transport, tool execution, raw evidence access. |

The MCP registry adapter design is tracked in
[`MCP_REGISTRY_ADAPTER_DESIGN_v0.6.md`](MCP_REGISTRY_ADAPTER_DESIGN_v0.6.md).
The MCP registry adapter fixture plan is tracked in
[`MCP_REGISTRY_ADAPTER_FIXTURE_PLAN_v0.6.md`](MCP_REGISTRY_ADAPTER_FIXTURE_PLAN_v0.6.md).
The MCP implementation gate design is tracked in
[`MCP_IMPLEMENTATION_GATE_DESIGN_v0.6.md`](MCP_IMPLEMENTATION_GATE_DESIGN_v0.6.md).
The local-only adapter dry-run skeleton is tracked in
`burp_ai_redaction_gateway/mcp_adapter_dry_run.py`. It consumes the registry,
adapter fixture, and implementation gate fixture without adding server,
transport, protocol handling, actual tool execution, a local evidence reader,
or runtime MCP exposure.
The local-only tool schema catalog is tracked in
[`MCP_LOCAL_ONLY_TOOL_SCHEMA_CATALOG_v0.6.md`](MCP_LOCAL_ONLY_TOOL_SCHEMA_CATALOG_v0.6.md)
and `burp_ai_redaction_gateway/mcp_tool_schema_catalog.py`. It derives
descriptor metadata from the registry and dry-run fixtures without adding
server, transport, protocol handling, actual tool execution, a local evidence
reader, or runtime MCP exposure.
The MCP runtime boundary decision is tracked in
[`MCP_RUNTIME_BOUNDARY_DECISION_v0.6.md`](MCP_RUNTIME_BOUNDARY_DECISION_v0.6.md).
It must be reviewed before any server, transport, protocol, tool execution, or
local evidence reader work starts.
The MCP server skeleton preflight is tracked in
[`MCP_SERVER_SKELETON_PREFLIGHT_v0.6.md`](MCP_SERVER_SKELETON_PREFLIGHT_v0.6.md).
It is a document, fixture, and test slice only; it does not add listener,
transport, protocol handling, tool execution, or local evidence reading.
The MCP runtime boundary consumption fixture is tracked in
[`MCP_RUNTIME_BOUNDARY_CONSUMPTION_v0.6.md`](MCP_RUNTIME_BOUNDARY_CONSUMPTION_v0.6.md).
It keeps the next listener-facing step limited to fixture, test, and
source-check evidence only.
The MCP listener skeleton decision is tracked in
[`MCP_LISTENER_SKELETON_DECISION_v0.6.md`](MCP_LISTENER_SKELETON_DECISION_v0.6.md).
It is a design and acceptance criteria document only. It does not implement or
approve a listener, transport, protocol handling, tool execution, or local
evidence reading.
The MCP listener skeleton acceptance criteria are tracked in
[`MCP_LISTENER_SKELETON_ACCEPTANCE_v0.6.md`](MCP_LISTENER_SKELETON_ACCEPTANCE_v0.6.md).
They add fixture and source-check policy only. They do not implement or approve
a listener, transport, protocol handling, tool execution, or local evidence
reading.
The MCP listener runtime-facing source check guard is tracked in
[`MCP_LISTENER_RUNTIME_SOURCE_CHECK_v0.6.md`](MCP_LISTENER_RUNTIME_SOURCE_CHECK_v0.6.md).
It keeps future listener-facing files declared before source-check scope can
expand. It still does not implement or approve listener, transport, protocol
handling, tool execution, or local evidence reading.
The v0.6 RC readiness checklist is tracked in
[`V0.6_RC_READINESS_CHECKLIST.md`](V0.6_RC_READINESS_CHECKLIST.md). It records
gate, smoke, UX, output bundle, MCP boundary, blocker, tag, and GitHub Release
decision criteria without creating a tag or release.

## Completed Read-Only UX Slice

The read-only dashboard UX bundle has landed on `main` as a completed v0.6
slice. It delivered:

- Output alias copy that keeps aliases separate from local paths and target
  identifiers.
- Troubleshooting categories linked only to existing dashboard routes.
- Release readiness status metadata with document filenames shown as
  non-serving reference text.
- Tests and browser smoke for no POST forms, no buttons, no `href="docs/`, and
  no raw or credential markers.

This completed slice does not approve tags, GitHub Releases, MCP runtime work,
or state-changing dashboard actions.

## Two-Week Candidate Sequence

This schedule is a planning aid, not a commitment.

1. Keep the merged output alias selector and read-only dashboard UX bundle as
   completed baseline slices.
2. Add or refresh this fast-track plan and its document hygiene test.
3. Draft the MCP registry adapter design and fixture matrix.
4. Fix the MCP runtime boundary decision before any server work.
5. Add the MCP server skeleton preflight before listener work.
6. Add the MCP runtime boundary consumption fixture before listener work.
7. Add the MCP listener skeleton decision before listener implementation.
8. Add the MCP listener skeleton acceptance criteria before listener
   implementation.
9. Add the v0.6 RC readiness checklist before release-management decisions.
10. Defer any runtime-affecting implementation until the design and fixtures are
   reviewed.

## Required Completion Criteria Per PR

Each PR in this track should include:

- One narrow read-only objective.
- Tests for the new document, route, or fixture.
- Browser or smoke evidence when a dashboard surface changes.
- Raw-free wording that avoids guarantee language.
- Explicit confirmation that no state-changing action was added.
- Explicit confirmation that no tag or GitHub Release was created.
- `git diff --check` and the standard local gates for the changed area.

## Work That Must Stay Separate

The following work changes security, transport, state, or raw-data boundaries.
Keep each as a separate design or implementation PR.

- MCP server implementation.
- MCP transport implementation.
- Tool handler implementation.
- Local evidence reader.
- Upload or import evidence action.
- Dashboard POST action.
- Collector forwarding change.
- Receiver ingest change.
- Raw preview or raw download.
- Replay.
- Active scan.
- Automatic ChatGPT handoff.
- File delete or retention behavior change.
- HMAC behavior change.
- CSRF behavior change.

## Boundary Checklist

Before moving any v0.6 PR out of Draft, confirm:

- The change is read-only unless the PR is explicitly scoped as a
  state-changing security review.
- The page, document, or fixture uses aliases and counts instead of local
  paths, target identifiers, raw traffic, or credential values.
- The four AI candidate safe files remain the only AI input candidates.
- Candidate findings are not treated as confirmed issues.
- Draft risk values are not treated as final severity.
- Severity and CVSS decisions remain manual.
- Passing smoke evidence is readiness evidence only.
- Tags and GitHub Releases remain separate release-management actions.

## Deferred Decisions

- Whether a read-only MCP prototype can ship without any local evidence reader.
- Which v0.6 slice should become the first runtime-affecting PR.
- What additional browser smoke should be required before state-changing
  dashboard actions are considered.
