# v0.7 Final Gate Execution

This document records the v0.7 final gate execution evidence before any later
tag or GitHub Release action. It is a release-readiness evidence document only.
It does not create a tag, create a GitHub Release, start a listener, open
transport, parse protocol messages, execute tools, read local evidence, or
change the four-file AI input candidate output bundle.
It does not create a tag and does not create a GitHub Release.

## Purpose

- Recheck the release approval packet target rule after PR #118 was merged.
- Record final gate results as raw-free metadata.
- Confirm v0.7 tag and GitHub Release absence before any later approval step.
- Keep the release execution decision separate from evidence collection.

## Current target candidate

The current target candidate is main HEAD after PR #118:

- PR #118 merge commit: `2c9829f6c21793e5b980add7bf1ecbeccc759a67`
- Target rule: `approval_packet_merge_commit_or_later_explicit_main_head`
- Target commit must be rechecked before release: yes

This candidate is not a release action. A later human approval must still name
the final target commit before any tag or GitHub Release command is run.

## Final gate scope

The final gate scope includes:

- Python compile check.
- Full unittest suite.
- Verification of the existing output directory.
- Review and report draft generation for the demo output.
- Montoya collector Gradle build.
- Secret scan for working tree and git history.
- Git safety check.
- Whitespace diff check.
- Clean working tree check.
- v0.7 tag absence check.
- GitHub Release v0.7 absence check.
- v0.6 tag and GitHub Release unchanged check.

## Executed gates

The following gates are expected for this final gate execution:

- `compileall`
- `unittest`
- `verify_out`
- `review_demo`
- `report_demo`
- `montoya_gradle_build`
- `gitleaks_dir`
- `gitleaks_git`
- `git_safety_check`
- `git_diff_check`
- `git_status_clean`
- `release_body_hygiene`
- `tag_target_verification`
- `release_target_verification`

Each gate result is represented in
`tests/fixtures/v07_final_gate_execution.json`. Gate evidence must stay
raw-free and must not copy target identifiers, credential values, local paths,
safe file bodies, or stack trace bodies into release evidence.

## Tag and release absence checks

The final gate requires:

- No local v0.7 tag.
- No remote v0.7 tag.
- No GitHub Release v0.7.
- Existing v0.6 tag remains unchanged.
- Existing GitHub Release v0.6 remains unchanged.

These checks do not create, move, or publish any tag or GitHub Release.

## Release body hygiene checks

A later release body must preserve these rules:

- No raw request or response content.
- No actual target identifiers.
- No credential values.
- No HMAC secret value.
- No CSRF token value.
- No full local paths.
- No safety guarantee language.
- No verified vulnerability claim.
- No fixed CVSS claim.
- Candidate findings only.
- Draft risk only.
- Manual review required.
- No automatic ChatGPT handoff claim.

## Runtime and deferred scope checks

The final gate does not implement or approve:

- Actual listener startup.
- Socket bind, listen, or accept behavior.
- Long-running server loop behavior.
- MCP transport.
- MCP protocol handler.
- JSON-RPC parser.
- Executable tool registration.
- Actual tool execution.
- Local evidence reader.
- Safe file body reader.
- Raw preview or raw download.
- Replay or active scan.
- Dashboard start or stop control.
- Upload or import action.
- Automatic ChatGPT handoff.
- Arbitrary local file read.
- External network call.
- Environment secret access.

## Go/hold decision input

If every gate passes, this document can support a later go/hold discussion for
v0.7 release execution. It is not the approval itself. The release action stays
blocked until a later separate explicit human approval names the final target
commit and authorizes tag and GitHub Release creation.

## Explicit non-goals

This PR does not:

- Create or move any tag.
- Create or modify any GitHub Release.
- Modify the v0.6 tag.
- Modify GitHub Release v0.6.
- Create the v0.7 tag.
- Create GitHub Release v0.7.
- Add runtime listener behavior.
- Add transport or protocol behavior.
- Register or execute tools.
- Read local evidence or safe file bodies.
- Add raw preview, replay, active scan, dashboard start or stop control, upload
  or import action, or automatic ChatGPT handoff.

## Next approval boundary

After this final gate evidence is merged, the next possible steps are:

- Draft v0.7 release notes without publishing a release.
- Request explicit v0.7 tag and GitHub Release approval.
- Hold release execution if main HEAD changes or any gate becomes stale.

Any later release execution must recheck main HEAD, v0.7 tag absence, GitHub
Release v0.7 absence, release body hygiene, and the selected target commit.
