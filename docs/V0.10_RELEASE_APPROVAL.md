# v0.10 Release Approval Decision

## Purpose

This document records the release approval decision input for v0.10 after the
release readiness PR was merged to main. It is not tag approval and it does not
create or publish a GitHub Release.

This document is release governance evidence. It is not a runtime feature
approval. It does not authorize tag or GitHub Release creation by itself.

v0.10 remains a metadata and safety-boundary release candidate. It does not
include transport runtime, listener startup runtime, protocol handling, tool
execution, local evidence reading, raw preview/download, replay, active scan,
or automatic ChatGPT handoff.

## Decision status

- Decision: `PENDING`
- Target commit: `0c115dc68f8cd4ea0a51aa1616a1f2af55a9f490`
- Release title candidate: `v0.10`
- Release tag: not created
- GitHub Release: not created
- Manual approval required before tag creation: true
- Manual approval required before GitHub Release publication: true
- Raw data included: false

## Current baseline

- PR #159: merged
- PR #159 merge commit: `0c115dc68f8cd4ea0a51aa1616a1f2af55a9f490`
- v0.10 release readiness document: merged to main
- v0.10 release readiness fixture: merged to main
- Runtime source changes in this approval document: none
- v0.10 tag check: no tag recorded for this approval step
- GitHub Release v0.10 check: no release recorded for this approval step

## Target commit decision

The release target must be confirmed before any tag is created.

Current recommendation: use Option A unless the maintainer explicitly wants the
approval document inside the v0.10 source archive. This keeps the v0.10 tag on
the release readiness baseline that was already merged and verified, while this
document remains post-readiness governance evidence.

Option A:

- v0.10 tag target:
  `0c115dc68f8cd4ea0a51aa1616a1f2af55a9f490`
- Meaning: the tag points at the PR #159 merge commit.
- Approval document status: governance evidence outside the v0.10 source
  archive.
- Required before execution: release approver must explicitly confirm that
  release governance evidence may live outside the tagged archive.

Option B:

- v0.10 tag target: a later commit that includes this approval document.
- Meaning: the approval document is included in the v0.10 source archive.
- Required before execution: update this document with the final target commit,
  rerun the full release gate on that commit, and confirm that moving the target
  beyond PR #159 is intentional.

This document keeps the decision at `PENDING`. It does not choose Option A or
Option B by itself.

## Tag protection and ruleset status

`gh ruleset list --repo ryuyunseong/burp-ai-redaction-gateway` returned no
ruleset entries during this approval-document preparation. No repository
settings were changed.

If tag protection, immutable release policy, or repository rulesets are required
for v0.10, configure and verify them in a separate approved repository-settings
task before publication.

## Release scope

v0.10 is a metadata and safety-boundary release for the MCP transport/listener
path. It includes:

- v0.10 scope decision
- transport/listener approval packet
- listener startup negative fixture harness
- listener startup implementation decision
- disabled-by-default listener startup metadata skeleton
- listener startup implementation review
- transport runtime decision
- release readiness record
- release approval decision input

## Explicitly excluded scope

The following surfaces are not part of v0.10:

- actual transport runtime
- actual listener startup runtime
- socket bind/listen/accept behavior
- HTTP server runtime
- Streamable HTTP runtime
- stdio subprocess runtime
- protocol handler
- protocol message parsing
- executable tool registration
- actual tool execution
- local evidence reader
- safe file body reader
- raw preview/download
- replay or active scan
- dashboard state-changing action
- upload/import action
- automatic ChatGPT handoff

## Required gate evidence before approval

Before changing the decision from `PENDING`, the following commands must pass on
the final target commit:

- `python -m compileall burp_ai_redaction_gateway tests`
- `python -m unittest discover -s tests`
- `gitleaks dir -v --redact=100 --config .gitleaks.toml .`
- `gitleaks git -v --redact=100 --config .gitleaks.toml .`
- `scripts\git_safety_check.bat`
- `git diff --check`
- `git status --short --untracked-files=all`
- `git tag --list "v0.10*"`
- `gh release view v0.10`

The tag and release checks must confirm that publication has not happened before
explicit approval.

## Approval conditions

The decision may move to `APPROVE RELEASE` only if all of the following are
true:

- the target commit is still `0c115dc68f8cd4ea0a51aa1616a1f2af55a9f490`;
- the approver has chosen Option A or Option B from the target commit decision
  section;
- the full gate set passes on the final target commit;
- no v0.10 tag exists before approval;
- no GitHub Release v0.10 exists before approval;
- release notes state that v0.10 is a metadata and safety-boundary release;
- release notes state that transport/listener runtime remains deferred;
- release notes do not claim actual runtime startup;
- release notes do not include raw traffic, credential values, target
  identifiers, local evidence bodies, stack traces, or local-only file names;
- release publication is separately and explicitly approved.

## Hold conditions

Keep the decision at `PENDING` or move it to `HOLD RELEASE` if any of the
following are true:

- the target commit changes without updating this decision document;
- the target commit option remains unresolved;
- any gate fails;
- a v0.10 tag already exists without recorded approval;
- a GitHub Release v0.10 already exists without recorded approval;
- deferred runtime scope is introduced;
- release notes imply transport runtime, listener startup runtime, protocol
  handling, tool execution, or local evidence reading;
- release notes include raw traffic, credential values, target identifiers,
  local evidence bodies, stack traces, or local-only file names;
- tag protection or repository ruleset requirements are unresolved and the
  maintainer wants those controls checked before publication.

## Release body draft

Title: `v0.10`

Draft body:

```markdown
v0.10 is a metadata and safety-boundary release for the MCP
transport/listener path.

This release records the v0.10 scope decision, transport/listener approval
packet, listener startup negative fixture harness, listener startup
implementation decision, disabled-by-default metadata skeleton, listener
startup implementation review, transport runtime decision, and release
readiness boundary.

It does not introduce transport/listener runtime.
Runtime transport/listener work remains deferred.
It does not introduce socket bind/listen/accept behavior, HTTP server runtime,
Streamable HTTP runtime, stdio subprocess runtime, protocol handling, protocol
message parsing, executable tool registration, actual tool execution, local
evidence reading, raw preview/download, replay/active scan, dashboard
state-changing controls, upload/import actions, or automatic ChatGPT handoff.

No raw Burp traffic should be included in release notes, fixtures, or examples.
```

## Tag and GitHub Release execution rule

This document does not authorize tag creation or GitHub Release publication by
itself. The final release step requires a separate explicit approval that names:

- the tag name;
- the exact target commit;
- the release title;
- the release body source;
- whether publication should proceed.

Do not run `gh release create v0.10` by itself. That command can create a tag at
an unintended default-branch target if no matching tag already exists.

Use one of these release execution patterns only after explicit approval:

- create the annotated tag at the approved commit first, push that tag, and run
  `gh release create v0.10 --verify-tag`; or
- run `gh release create v0.10 --target <approved-commit-sha>` with the approved
  release body source.

After publication, verify that the `v0.10` tag points to the approved commit and
that the GitHub Release body still states that v0.10 does not include transport
or listener runtime.

## Rollback plan

If this decision document is wrong, revert only the decision-document change. If
a later tag or GitHub Release is created on the wrong target, stop publication,
record the mismatch, and request explicit maintainer approval before any
corrective release action.

## Next step

The next step is either:

- `APPROVE RELEASE` with the exact target commit and release body; or
- `HOLD RELEASE` with the reason and required correction.
