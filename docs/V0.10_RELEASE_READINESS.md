# v0.10 Release Readiness

## Purpose

This document records the v0.10 release readiness boundary before any v0.10 tag
or GitHub Release is created. It closes v0.10 as a metadata and safety-boundary
release without actual transport runtime, listener startup runtime, protocol
handling, tool execution, or local evidence reading.

## Release target

- Release candidate commit:
  `24ecb841fa59104d7bad72f5e8dea11388ee281c`
- Release tag: not created
- GitHub Release: not created
- Readiness status: candidate review only
- Manual review: required before tag or release publication
- Raw data included: false

## v0.10 included scope

v0.10 includes planning, fixture, metadata-only helper, source-check, and
release-boundary work for the MCP transport/listener path. It does not include
actual runtime startup.

The included scope is:

- `v010_scope_decision`
- `transport_listener_approval_packet`
- `listener_startup_negative_fixture_harness`
- `listener_startup_implementation_decision`
- `listener_startup_metadata_skeleton`
- `listener_startup_implementation_review`
- `transport_runtime_decision`
- `source_check_hardening`
- `metadata_only_runtime_absence_boundary`

## v0.10 explicit deferred scope

The following surfaces remain deferred and are not part of v0.10:

- `actual_transport_runtime`
- `actual_listener_startup`
- `socket_bind`
- `socket_listen`
- `socket_accept`
- `http_server`
- `streamable_http_runtime`
- `stdio_subprocess_runtime`
- `protocol_handler`
- `protocol_message_parsing`
- `executable_tool_registration`
- `actual_tool_execution`
- `local_evidence_reader`
- `safe_file_body_reader`
- `raw_preview_download`
- `replay_active_scan`
- `automatic_chatgpt_handoff`
- `dashboard_state_changing_action`
- `upload_import_action`

## Included PRs

The v0.10 release readiness baseline includes:

- #152
- #153
- #154
- #155
- #156
- #157
- #158

## Required gate evidence

The release candidate must keep these gates green before tag or release
approval:

- `python -m compileall burp_ai_redaction_gateway tests`
- `python -m unittest discover -s tests`
- `python -m unittest discover -s tests -k v10_release_readiness`
- `gitleaks dir -v --redact=100 --config .gitleaks.toml .`
- `gitleaks git -v --redact=100 --config .gitleaks.toml .`
- `scripts\git_safety_check.bat`
- `git diff --check`
- `git status --short --untracked-files=all`

## Security and privacy checklist

- No v0.10 tag before explicit release approval.
- No GitHub Release v0.10 before explicit release approval.
- No actual transport runtime.
- No actual listener startup runtime.
- No socket bind, listen, or accept behavior.
- No HTTP server or Streamable HTTP runtime.
- No stdio subprocess runtime.
- No protocol handler or protocol message parser.
- No executable tool registration.
- No actual tool execution.
- No local evidence reader or safe file body reader.
- No raw preview/download.
- No replay or active scan.
- No dashboard state-changing control.
- No upload/import action.
- No automatic ChatGPT handoff.

## Runtime absence checklist

The v0.10 readiness candidate must continue to show:

- `v010_actual_runtime_included=false`
- `release_tag_created=false`
- `github_release_created=false`
- `manual_review_required=true`
- `raw_data_included=false`
- `v010_should_not_add_more_planning_prs=true`
- `approved_next_step=v010_release_approval`

## Raw-free assurance limits

This readiness record is metadata-only. It does not include raw traffic,
credential values, target identifiers, local evidence bodies, local-only file
names, stack traces, or release artifacts.

The readiness record does not clear any output for external distribution. A
human reviewer must confirm the final release notes, target commit, gate
evidence, and deferred scope before a tag or GitHub Release is created.

## Tag and GitHub Release preconditions

Before a v0.10 tag or GitHub Release can be created:

- the target commit must match the release candidate commit in this readiness
  record or this readiness record must be updated;
- the full gate set must be rerun on the final target commit;
- no deferred runtime surface may be introduced;
- release notes must include actual runtime absence;
- release notes must include the deferred scope;
- release notes must not claim transport runtime;
- release notes must not claim listener runtime;
- release notes must not claim protocol handler behavior;
- release notes must not claim tool execution;
- release notes must not claim local evidence reader behavior;
- tag creation and GitHub Release publication require separate explicit
  approval.

## Release notes draft

Draft title: v0.10

Draft scope:

- v0.10 is a metadata and safety-boundary release for the MCP
  transport/listener path.
- v0.10 includes the scope decision, transport/listener approval packet,
  listener startup negative fixture harness, startup implementation decision,
  metadata-only listener startup skeleton, implementation review, and transport
  runtime decision.
- v0.10 does not include actual transport runtime, listener startup runtime,
  socket bind/listen/accept behavior, HTTP server runtime, Streamable HTTP
  runtime, stdio subprocess runtime, protocol handler behavior, protocol message
  parsing, executable tool registration, actual tool execution, local evidence
  reading, raw preview/download, replay/active scan, dashboard state-changing
  controls, upload/import actions, or automatic ChatGPT handoff.
- v0.10 release publication must use the final reviewed release target commit.

## Rollback plan

If the readiness PR is incorrect, revert only the readiness PR. If a later v0.10
tag or GitHub Release is created on the wrong commit, stop publication, record
the mismatch, and request explicit maintainer approval before any corrective
release action.

## Final decision

Readiness PR approval is not tag approval. v0.10 tag creation and GitHub Release
publication remain blocked until a separate explicit approval confirms the
target commit and release body.

The next step after this readiness PR is `v010_release_approval`.

## Deferred work for v0.11 or later

- actual transport runtime
- actual listener startup runtime
- socket bind/listen/accept behavior
- HTTP server runtime
- Streamable HTTP runtime
- stdio subprocess runtime
- protocol handler
- protocol message parser
- executable tool registration
- actual tool execution
- local evidence reader
- safe file body reader
- raw preview/download
- replay or active scan
- dashboard state-changing action
- upload/import action
- automatic ChatGPT handoff
