# v0.8 Final Gate Evidence

This document records v0.8 final gate evidence only. It does not create a tag,
does not create a GitHub Release, does not implement runtime behavior, and does
not approve release execution by itself.

## Purpose

- Fix the release target candidate after the release approval packet was merged.
- Record the final gate commands and raw-free results before any later release
  action.
- Keep v0.8 positioned as a boundary/helper release, not as a read-only MCP
  runtime release.
- Keep tag creation and GitHub Release publication blocked until separate
  explicit human approval.

## Final Gate Target Commit

The final gate target commit is:

`c5ff5c396d7e84a25d94d9ef12aeb85a03ac6b06`

This is the main HEAD checked for this final gate. A later release action must
target this commit or a later explicitly named main HEAD after rerunning the
required target and hygiene checks.

## Approval Packet Target Drift Note

The release approval packet target commit was:

`58c7190e5cde43ca2ae4bddf056360eda649622b`

That value was the main HEAD before PR #133 was merged. This final gate
rechecks main after PR #133 and fixes the current release target candidate as
`c5ff5c396d7e84a25d94d9ef12aeb85a03ac6b06`.

## Baseline PRs

- PR #130 merge commit `fcc0dd947ec9c62410cc5715cce7ae623bc32aa8`
- PR #131 merge commit `bb484ea21d90759125a7224fedaec6a9ee7b75fc`
- PR #132 merge commit `58c7190e5cde43ca2ae4bddf056360eda649622b`
- PR #133 merge commit `c5ff5c396d7e84a25d94d9ef12aeb85a03ac6b06`

## Release Type

The release type remains `boundary_helper`.

v0.8 is not a read-only MCP runtime release. Listener startup, socket behavior,
transport, protocol parsing, request dispatch, executable tool registration,
actual tool execution, and local evidence reading remain deferred.

## Included Scope

- redaction, verify, review, and report pipeline
- four AI input candidate files
- dashboard read-only and raw-free baseline
- disabled-by-default MCP helper files
- post-merge boundary note
- scope freeze and RC readiness evidence
- release approval packet

## Excluded Scope

- listener startup
- socket, bind, listen, or accept behavior
- stdio runtime
- HTTP runtime
- protocol parser
- JSON-RPC parser
- request dispatcher
- executable tool registration
- tool registry runtime
- tool discovery runtime
- actual tool execution
- local evidence reader
- safe file body reader
- raw preview or download
- replay or active scan
- dashboard state-changing control
- upload or import action
- automatic ChatGPT handoff

## Final Gate Results

- Python compile check: passed
- Python unit test discovery: passed
- gitleaks directory scan: passed
- gitleaks git scan: passed
- git safety check: passed
- diff whitespace check: passed
- working tree check: clean
- v0.8 tag check: absent
- GitHub Release v0.8 check: absent

These results are evidence only. They do not create, move, or publish a tag or
release.

## Final Gate Commands

- `python -m compileall burp_ai_redaction_gateway tests`
- `python -m unittest discover -s tests`
- `gitleaks dir -v --redact=100 --config .gitleaks.toml .`
- `gitleaks git -v --redact=100 --config .gitleaks.toml .`
- `scripts\git_safety_check.bat`
- `git diff --check`
- `git status --short --untracked-files=all`
- `git tag --list 'v0.8*'`
- `gh release view v0.8`

## Release Body Hygiene Requirements

A later release body must clearly state:

- v0.8 is a boundary/helper release.
- v0.8 is not a read-only MCP runtime release.
- Runtime, parser, dispatcher, tool execution, and local evidence reader
  surfaces are deferred.
- Candidate findings remain candidates.
- Risk remains draft.
- Severity and scoring remain manual decisions.
- AI input candidates remain limited to the four-file output bundle.

The release body must not include:

- raw request or response content
- actual target identifiers
- credential values
- HMAC key material
- CSRF credential value
- full local paths
- safety guarantee language
- verified finding claims
- fixed scoring claims
- automatic ChatGPT handoff claims

## Rollback Requirements

- Before a tag exists, rollback means reverting the final gate evidence PR and
  rechecking main HEAD.
- After a tag or GitHub Release exists, rollback requires a separate explicit
  rollback plan.
- Do not move tags as part of this evidence document.
- Do not mutate GitHub Releases as part of this evidence document.

## Human Approval Boundary

This evidence can support a later release approval discussion, but it is not the
approval itself. A human must explicitly approve the v0.8 target commit, tag
creation, and GitHub Release action after reviewing the final release body.

## Deferred Work

- read-only MCP runtime release decision
- protocol parser approval packet
- protocol negative harness execution
- transport approval packet
- dispatcher approval packet
- local evidence reader approval packet
- dashboard state-changing action approval packet
- upload or import action approval packet
- automatic ChatGPT handoff decision
